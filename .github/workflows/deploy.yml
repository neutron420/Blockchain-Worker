# name: CI/CD Pipeline - Build, Test & Deploy to AWS ECS

# on:
#   push:
#     branches:
#       - main
#       - develop
#   pull_request:
#     branches:
#       - main
#       - develop

# env:
#   AWS_REGION: us-east-1
#   ECR_REPOSITORY: grievance-blockchain-worker
#   ECS_SERVICE: grievance-blockchain-worker
#   ECS_CLUSTER: grievance-cluster
#   ECS_TASK_DEFINITION: grievance-blockchain-worker
#   CONTAINER_NAME: grievance-blockchain-worker

# jobs:
#   test:
#     name: Run Tests
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
        
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#           cache: 'npm'
          
#       - name: Install dependencies
#         run: npm ci
        
#       - name: Run Solidity compilation
#         run: npm run compile
        
#       - name: Run tests
#         run: npm test
        
#       - name: Check for TypeScript errors
#         run: npx tsc --noEmit
        
#   build:
#     name: Build & Push to ECR
#     runs-on: ubuntu-latest
#     needs: test
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
#     outputs:
#       image: ${{ steps.image.outputs.image }}
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
        
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}
          
#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2
        
#       - name: Build Docker image
#         id: docker-build
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
#           docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .
#           echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
          
#       - name: Push Docker image to ECR
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest
          
#       - name: Set image output
#         id: image
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
          
#   deploy:
#     name: Deploy to AWS ECS
#     runs-on: ubuntu-latest
#     needs: build
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
        
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}
          
#       - name: Download task definition
#         run: |
#           aws ecs describe-task-definition \
#             --task-definition $ECS_TASK_DEFINITION \
#             --query taskDefinition > task-definition.json
            
#       - name: Update task definition with new image
#         id: task-def
#         uses: aws-actions/amazon-ecs-render-task-definition@v1
#         with:
#           task-definition: task-definition.json
#           container-name: ${{ env.CONTAINER_NAME }}
#           image: ${{ needs.build.outputs.image }}
          
#       - name: Deploy to ECS
#         uses: aws-actions/amazon-ecs-deploy-task-definition@v2
#         with:
#           task-definition: ${{ steps.task-def.outputs.task-definition }}
#           service: ${{ env.ECS_SERVICE }}
#           cluster: ${{ env.ECS_CLUSTER }}
#           wait-for-service-stability: true
          
#       - name: Verify deployment
#         run: |
#           echo "✅ Deployment to ECS completed successfully"
#           aws ecs describe-services \
#             --cluster $ECS_CLUSTER \
#             --services $ECS_SERVICE \
#             --query 'services[0].[serviceName,status,runningCount,desiredCount]' \
#             --output table
            
#   notify:
#     name: Send Deployment Notification
#     runs-on: ubuntu-latest
#     needs: [test, build, deploy]
#     if: always()
    
#     steps:
#       - name: Check deployment status
#         run: |
#           if [[ "${{ needs.deploy.result }}" == "success" ]]; then
#             echo "✅ Deployment successful!"
#             exit 0
#           elif [[ "${{ needs.deploy.result }}" == "skipped" ]]; then
#             echo "ℹ️ Deployment skipped (not on main branch)"
#             exit 0
#           else
#             echo "❌ Deployment failed!"
#             exit 1
#           fi
          
#       - name: Slack notification (on failure)
#         if: failure()
#         uses: slackapi/slack-github-action@v1
#         with:
#           payload: |
#             {
#               "text": "❌ Blockchain Worker CI/CD Pipeline Failed",
#               "blocks": [
#                 {
#                   "type": "section",
#                   "text": {
#                     "type": "mrkdwn",
#                     "text": "*Build Failed* - Blockchain Worker\n*Repository:* ${{ github.repository }}\n*Branch:* ${{ github.ref }}\n*Commit:* ${{ github.sha }}\n*Author:* ${{ github.actor }}"
#                   }
#                 }
#               ]
#             }
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#           SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
          
#       - name: Slack notification (on success)
#         if: success() && github.event_name == 'push' && github.ref == 'refs/heads/main'
#         uses: slackapi/slack-github-action@v1
#         with:
#           payload: |
#             {
#               "text": "✅ Blockchain Worker Deployed to AWS ECS",
#               "blocks": [
#                 {
#                   "type": "section",
#                   "text": {
#                     "type": "mrkdwn",
#                     "text": "*Deployment Successful* ✅\n*Service:* grievance-blockchain-worker\n*Cluster:* grievance-cluster\n*Region:* us-east-1\n*Commit:* ${{ github.sha }}\n*Author:* ${{ github.actor }}"
#                   }
#                 }
#               ]
#             }
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#           SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
